/**
 * HUD Frame — purely CSS-driven, nothing dynamic needed here.
 * The CSS in hud.css handles the corner brackets and border.
 */
class HudFrame {
  constructor() {
    // Static component — CSS handles everything
  }
}
